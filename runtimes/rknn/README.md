# RKNN runtime (placeholder)

Reserved for a future llama.cpp build targeting Rockchip NPUs (RKNN).
Same packaging pattern as `runtimes/jetson`:
release asset `llama-server-<tag>-linux-aarch64-rknn.tar.gz` under the same
`runtime-<tag>` release, SHA-pinned in the NeoMind client. Add the build
recipe here when the toolchain is validated (see RK1828 eval notes).
